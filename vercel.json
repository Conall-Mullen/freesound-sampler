{
  "version": 2,
  "builds": [
    {
      "src": "next.config.js",
      "use": "@vercel/node",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [{ "handle": "filesystem" }, { "src": "/.*", "dest": "/index.js" }]
}
